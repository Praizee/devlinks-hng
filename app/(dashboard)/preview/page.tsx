import Preview from "@/app/components/Dashboard/Preview";

export const metadata = {
  title: "Preview",
  description: "Preview of your public profile links",
};

const page = () => {
  return <Preview />;
};

export default page;
